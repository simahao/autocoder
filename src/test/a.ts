import { Utils } from '../utils';
describe('utils', () => {
    it('uppder', () => {
        // assert.equal('ClientId', Utils.upperFirstChar('clientId'));
        Utils.parseClasses("sf");
    });
});